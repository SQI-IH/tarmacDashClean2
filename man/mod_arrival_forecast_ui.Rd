% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_arrival_forecast.R
\name{mod_arrival_forecast_ui}
\alias{mod_arrival_forecast_ui}
\title{arrival forecast module (shinydashboard version)}
\usage{
mod_arrival_forecast_ui(id)

mod_arrival_forecast_ui(id)
}
\description{
Physician-facing forecast of whether a given patient type (e.g., CTAS 2–4, arrival mode)
will arrive in the next few hours. This version uses {shinydashboard} components.

Physician-facing forecast of whether a given patient type (e.g., CTAS 2–4, arrival mode)
will arrive in the next few hours. This version uses {shinydashboard} components.
}
\details{
Usage:
  In UI:     mod_arrival_forecast_ui("pred")
  In server: mod_arrival_forecast_server("pred", forecast_provider = fake_forecast_provider)

Contract for forecast_provider(filters): returns a tibble with columns
  hour (POSIXct), p, p_low, p_high, top_factors (list or JSON),
  bed_type (chr), imaging_likelihood (chr), consult (chr),
  calibration_brier (num), hist_base_rate (num in [0,1]),
  observed (optional numeric 0/1 for past truth, used in sparkline)

Requires in the app: library(shiny); library(shinydashboard); library(shinyjs);
  library(plotly); library(ggplot2); library(scales); library(jsonlite);
  library(lubridate); library(dplyr); library(tibble)

Usage:
  In UI:     mod_arrival_forecast_ui("pred")
  In server: mod_arrival_forecast_server("pred", forecast_provider = fake_forecast_provider)

Contract for forecast_provider(filters): returns a tibble with columns
  hour (POSIXct), p, p_low, p_high, top_factors (list or JSON),
  bed_type (chr), imaging_likelihood (chr), consult (chr),
  calibration_brier (num), hist_base_rate (num in [0,1]),
  observed (optional numeric 0/1 for past truth, used in sparkline)

Requires in the app: library(shiny); library(shinydashboard); library(shinyjs);
  library(plotly); library(ggplot2); library(scales); library(jsonlite);
  library(lubridate); library(dplyr); library(tibble)
}
